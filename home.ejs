<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Welcome, <%= username %>!</h1>
    <a href="/create-post">Create a post</a>
    <div>
        <% posts.forEach(post => { %>
            <div>
                <h2><%= post.username %></h2>
                <p><%= post.content %></p>
                <% if (post.imagePath) { %>
                    <img src="<%= post.imagePath %>" alt="Post image" style="max-width: 300px;">
                <% } %>
            </div>
        <% }) %>
    </div>
</body>
</html>